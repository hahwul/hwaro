# Backward compatibility wrapper for Init module
# The main implementation has moved to core/init/initializer.cr

require "./init/initializer"

# The Init class is now defined in init/initializer.cr with backward compatibility
