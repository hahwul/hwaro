# Backward compatibility wrapper for Serve module
# The main implementation has moved to core/serve/server.cr

require "./serve/server"

# The Serve class is now defined in serve/server.cr with backward compatibility
