### Changed
- Internal restructuring for parallel and AI-assisted development: the render phase, config model, dev server, builder, doctor, deployer, check-links and Markdown processor are split into per-concern files under a directory named after each owner file; config sections register in one `SECTION_LOADERS` table; duplicated pipelines (incremental re-parse, deploy directory sync, importer run loops, scaffold full-config assembly, search overlay script) are shared. No user-visible behaviour change; generated sites are byte-identical.
- Contributor tooling: `just test-file` / `just test-dir` (isolated compiler cache), `just verify` (byte-identity gate against a baseline binary), `just ameba`, `changelog.d/` fragments merged by `just changelog`, and `ARCHITECTURE.md` with a "where does X go" map.
